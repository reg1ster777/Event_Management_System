<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.program.programdesignb.mapper.StudentMapper">

    <resultMap id="StudentResultMap" type="com.program.programdesignb.domain.Student">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, name, age
    </sql>

    <select id="findById" parameterType="long" resultMap="StudentResultMap">
        select
        <include refid="BaseColumnList"/>
        from student
        where id = #{id}
    </select>

    <select id="findAll" resultMap="StudentResultMap">
        select
        <include refid="BaseColumnList"/>
        from student
    </select>

    <insert id="insert" parameterType="com.program.programdesignb.domain.Student"
            useGeneratedKeys="true" keyProperty="id">
        insert into student(name, age)
        values (#{name}, #{age})
    </insert>

    <update id="update" parameterType="com.program.programdesignb.domain.Student">
        update student
        set name = #{name},
            age = #{age}
        where id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        delete from student where id = #{id}
    </delete>

</mapper>
